/*


TODO:
-----

> character creation menu (sprites will be placeholders)
> aicore runs in the background during character creation menu is in use
 > loading bar with status in the bottom

> in character creation menu no ease is used for switching between options for parts of character
 > this thing is used instead, to make it more smooth switching between options:
  > x -= (x_to - x) / ease


*/